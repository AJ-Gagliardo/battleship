import React from "react";

const Cell = () => {
  return (
    <button
      // className={`${fired ? (hasShip ? "bg-amber-800" : "bg-slate-700") : "bg-slate-200"}`}
      className="bg-slate-200"
    >
      Cell
    </button>
  );
};

export default Cell;
